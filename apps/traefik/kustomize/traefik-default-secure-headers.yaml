## 入口為 websecure 的 IngressRoute，預設會加入 default-secure-headers Middleware，無須自行加入。
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: default-secure-headers
  namespace: default
spec:
  headers:
    stsSeconds: 63072000
    stsIncludeSubdomains: true
    forceSTSHeader: true
